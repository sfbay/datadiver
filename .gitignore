# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Environment variables
.env
.env.local
.env.*.local

# Planning files
task_plan.md
findings.md
progress.md

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.vercel
.worktrees
.superpowers

# Word working files
# Lock files (Word writes these next to open .docx files)
~$*
# Dated working copies of the compliance report. The canonical
# resolution-240210-compliance-report.docx (no date suffix) is tracked.
reports/resolution-240210-compliance-report-*.docx

# Design handoff working artifacts — the brief's content distills into the
# code itself (tokens.css, components, CLAUDE.md). The unzipped reference
# project + the zip drop in public/ are local working files only.
docs/design-handoff/
public/DataDiver Design System.zip
.env*.local
.superpowers/
